rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /rooms/{room} {
      allow create: if request.auth != null;
      allow read, write: if request.auth != null 
          && request.auth.token.email in resource.data.authorizedUsersEmail;
      match /stories/{story} {
        allow create, read, write:
          if request.auth != null && 
            request.auth.token.email in get(/databases/$(database)/documents/rooms/$(room)).data.authorizedUsersEmail; 
      }
    }
  }
}
